## 变更说明

（本次改动解决了什么问题，一行到几行即可）

## 测试

- [ ] `node test/self-test.js` 通过
- [ ] `node test/accuracy-test.js` 通过
- [ ] `node test/reliability-test.js` 通过
- [ ] `node test/fuzz-test.js` 通过

## 回归用例

（新增/修改的规则是否附带虚构数据回归用例？如有，简述场景）

## 版本同步（如涉及发版）

- [ ] `package.json` 与 `lib/client.js` 的 `PLUGIN_VERSION` 一致
- [ ] `CHANGELOG.md` 已更新
- [ ] `README.md` 测试计数已同步（如涉及）

## 已知影响

（行为变化、兼容性影响、需要用户注意的点；无则写「无」）
