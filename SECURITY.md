# 安全策略

dsh-privmask 以「**云端与本地日志不见明文**」为安全目标：敏感信息在发往云端前被替换为占位符，
本地会话日志在写入前同样遮罩。本项目处理的是隐私数据，安全缺陷的影响面高于一般 bug。

## 支持的版本

仅最新发布版本（npm `latest`）提供安全修复；旧版本请升级后使用。

## 报告漏洞

### 不适合公开的内容

以下内容**不要**贴在公开 Issue / PR / 讨论中：

- 包含真实姓名、证件号、联系方式、地址、账号等敏感信息的复现样例（请替换为虚构数据）
- 你本人或他人的真实隐私数据

### 如何报告

1. **私有通道**：通过 GitHub 仓库的
   [Security Advisories](https://github.com/JunyuZhan/dsh-privmask/security/advisories/new)
   提交（仅维护者可见）；或向仓库维护者发送邮件（见 GitHub 主页联系方式）。
2. 报告中请包含：
   - 影响的版本（卡片右上角「插件版本」或 `package.json`）
   - 复现步骤与**脱敏后的**输入样例（虚构数据即可）
   - 期望行为与实际行为的差异（例如：某字段明文到达模型 / 日志落盘明文）
   - 你认为的严重程度与影响面

## 已知的机制性边界

以下为设计取舍而非缺陷，供报告前参考：

- 入站还原后，**模型回复（assistant 消息）与工具调用参数在本地为真值**，云端与出站请求仍为占位符；
  如需日志完全无明文，请关闭 `restoreInbound`。
- dsh 平台 `agent/inbox/spliced` 事件在插件改写缝之前以原文落盘，privmask 无法遮罩该副本
  （详见 README「已知限制」）。
- 规则为启发式：词表外写法可能漏检；误报可用 `preserveValues` 白名单或关闭对应类别。

## 修复流程

- 确认漏洞后，维护者会优先发布补丁版本，并在 CHANGELOG 记录修复。
- 报告者可在修复版本发布后收到确认；如需署名，请告知 GitHub 用户名或昵称。
